# Ham Radio Dashboard Environment Configuration
# Copy this file to .env and customize the values

# Domain name for accessing the dashboard via Traefik
# Use a real domain for HTTPS with Let's Encrypt certificates
HAMDASH_DOMAIN=hamdash.yourdomain.com

# Traefik Docker network name (must match your existing Traefik network)
TRAEFIK_NETWORK=traefik-network

# Pi-hole DNS server IP address (optional)
# Leave as 1.1.1.1 if not using Pi-hole
PIHOLE_DNS=1.1.1.1

# Timezone
TZ=UTC

# For HTTPS with Let's Encrypt, ensure your Traefik instance has:
# 1. A certificate resolver named 'letsencrypt' configured
# 2. The 'websecure' entrypoint configured for port 443
# 3. The 'web' entrypoint configured for port 80
# 4. ACME email configured for certificate generation
